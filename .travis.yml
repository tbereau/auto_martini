sudo: true
language: python
python:
- 2.7
install:
- sudo apt-get install python-rdkit && pip install requests bs4 numpy cython
script:
- cd Auto_MARTINI && python setup.py install --user && cd .. && python -m auto_martini test
